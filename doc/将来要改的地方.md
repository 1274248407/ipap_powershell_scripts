# make it work

## 重要且紧急

[X] build.ps1检查一遍
[X] 改进文件夹分布，用[sampler规范](https://www.pr0mpt.com/2025-04-22-quickly-making-high-quality-powershell-modules-using-sampler/)
[X] 取消除了单元测试以外的所有测试项目
[X] \02_Preprocessing\original_non_text_raw高清化后会出现：原无文字图和高清化后的无文字图共存的情况

## 紧急但不重要
[X] 删除bin目录下不必要的文件
[X] 把Write-LogEntry.ps1和对应的单元测试搞过来

## 重要但不紧急

[X] 保证功能正常运行，测试都通过
[ ] 审核代码，保证静态检测工具无报错，测试覆盖率达到100%，运行项目后无错误，合并分支
[ ] 和ai讨论打包方案

## 不重要不紧急

[ ] 复制到02_Preprocessing\original_non_text_raw必须从1开始按从大到小顺序重新命名文件
[ ] 加入高清化失败重试的机制
[ ] 02_preprocessing加入创建result文件夹
[ ] 加上任务完成的提示音
[ ] 自动根据网站获取漫画名，作者等
[ ] 自动化翻译流程，估计要将readme.md改为toml结构
[ ] 了解用manganijia高清化图片,看能不能应用到这里

# make it right

## 重要且紧急
[ ] 全部使用ps7新语法

## 紧急但不重要
[ ] 想办法分离ipap.classes.ps1中的类定义，分别放到一个单独的ps1文件中

## 重要但不紧急
[ ] 参照Strongly Encouraged Development Guidelines去检查项目[https://learn.microsoft.com/en-us/powershell/scripting/developer/cmdlet/strongly-encouraged-development-guidelines?view=powershell-7.6]

## 不重要不紧急

1. [ ] pester框架迁移到v6版本
2. [ ] build.ps1的Invoke-OutputTypeAudit中有如下问题：
$parseErrors 未被检查
ParseFile 通过 [ref]$parseErrors 返回了解析错误，但代码未对其进行检查。若文件存在语法错误，$ast 可能为 $null 或结构不完整，后续 $ast.FindAll 将抛出 NullReferenceException
Write-Output 与 [OutputType([void])] 的语义冲突
函数声明了 [OutputType([void])]，表明不应有输出对象。但代码中使用 Write-Output 发送诊断信息，这会将字符串写入 输出流（Success Stream），与 void 声明在语义上矛盾
当前 $missing 使用字符串拼接，不便于后续程序化处理。可改为 PSCustomObject

[ ] remove-item全部改成移动到回收站
[ ] 测试全过
[ ] 用工具改进架构

# make it fast (80%的情况下不需要)
