# IPAP Workflow PowerShell 模块 - .gitignore 文件
# 基于 PowerShell 7 项目规范生成

################################################################################
# 编辑器元数据 - 防止本地开发环境配置污染版本库
################################################################################

# Trae IDE 生成的元数据
# 原因：Trae 会在 .trae/ 目录中存储本地开发环境配置、规范文档等
# 处理建议：这些文件是本地开发环境特有的，不应提交到版本库
.trae/

# VS Code 生成的元数据
# 原因：VS Code 会在 .vscode/ 目录中存储本地工作区配置、调试设置等
# 处理建议：这些文件是本地开发环境特有的，不应提交到版本库
.vscode/

################################################################################
# 配置文件 - 防止开发机器间的配置参数冲突
################################################################################

# 主配置文件
# 原因：config.toml 包含本地环境的路径配置和个性化设置，不同开发机器可能不同
# 处理建议：创建一个 config.toml.example 作为模板，供其他开发者参考
config.toml

################################################################################
# 运行时产物 - 避免提交冗余的运行时数据
################################################################################

# 日志文件
# 原因：运行脚本时可能生成的日志文件，包含运行时信息和错误信息
# 处理建议：这些文件是运行时生成的，不应提交到版本库
*.log

# 临时文件
# 原因：运行脚本时可能生成的临时文件，用于存储中间数据
# 处理建议：这些文件是临时的，不应提交到版本库
*.tmp
# 二进制依赖目录
/bin/
# 项目生成的文档
# 原因：运行工作流时会在项目目录中生成 README.md、project_brief.md 等文档
# 处理建议：这些文件是运行时生成的，不应提交到版本库
**/README.md
**/project_brief.md
**/glossary.json

# 处理后的图像文件
# 原因：运行工作流时会生成处理后的图像文件，包括高清化和 WebP 转换后的文件
# 处理建议：这些文件是运行时生成的，不应提交到版本库
**/02_Preprocessing/upscaled/
**/02_Preprocessing/webp/

################################################################################
# Windows 系统垃圾 - 排除 Windows 10 环境下的系统生成文件
################################################################################

# Thumbs.db 文件
# 原因：Windows 资源管理器生成的缩略图缓存文件
# 处理建议：这些文件是系统生成的，不应提交到版本库
Thumbs.db

# desktop.ini 文件
# 原因：Windows 资源管理器生成的文件夹配置文件
# 处理建议：这些文件是系统生成的，不应提交到版本库
desktop.ini

# 回收站文件
# 原因：Windows 回收站生成的文件
# 处理建议：这些文件是系统生成的，不应提交到版本库
$RECYCLE.BIN/

# 系统卷信息
# 原因：Windows 系统卷信息文件夹
# 处理建议：这些文件是系统生成的，不应提交到版本库
System Volume Information/

################################################################################
# 工具目录 - 排除第三方工具和模型
################################################################################

# RealCUGAN 工具目录
# 原因：realcugan-ncnn-vulkan 是第三方工具，体积较大
# 处理建议：建议在 README 中说明如何下载和安装此工具，而不是将其提交到版本库
realcugan-ncnn-vulkan-20220728-windows/


################################################################################
# PowerShell 模块相关 - 排除模块安装和缓存文件
################################################################################

# PowerShell 模块缓存
# 原因：PowerShell 会在模块安装时生成缓存文件
# 处理建议：这些文件是运行时生成的，不应提交到版本库
*.psd1.cache

# 模块安装目录
# 原因：PowerShell 模块安装到用户目录时生成的文件
# 处理建议：这些文件是本地安装的，不应提交到版本库
$HOME\Documents\PowerShell\Modules/

################################################################################
# 其他临时文件 - 排除可能的临时文件
################################################################################

# 备份文件
# 原因：编辑器或系统可能生成的备份文件
# 处理建议：这些文件是临时的，不应提交到版本库
*.bak
*.backup

# 临时文件夹
# 原因：可能的临时文件夹
# 处理建议：这些文件夹是临时的，不应提交到版本库
temp/
tmp/
# 忽略 .gitignore 文件
# 原因：.gitignore 是版本控制系统的配置文件，不应提交到版本库
.gitignore
# 打包目录
IPAP_Package/
*.zip
TestResults.json
TestResults.xml
CodeCoverage.xml
vendor/PoShLog/2.1.1/PSGetModuleInfo.xml
vendor/